#include <iostream>
#include <vector>
#include <algorithm>
#include <iomanip>

using namespace std;

void findHail(vector <int>&, int numb);
void print(const vector <int>);
int length(const vector <int>);
int largest(const vector <int>);
int smallest(const vector <int>);
void odds(const vector <int>);
void evens(const vector <int>);


